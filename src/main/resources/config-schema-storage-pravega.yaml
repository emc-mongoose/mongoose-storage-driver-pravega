---
storage:
  driver:
    create:
      timestamp: boolean
    control:
      scope: boolean
      stream: boolean
      timeoutMillis: long
    event:
      transaction: boolean
      key:
        enabled: boolean
        count: long
      timeoutMillis: long
    read:
      tail: boolean
    scaling:
      type: string
      rate: int
      factor: int
      segments: int
    stream:
      data: string
  namespace: string
  net:
    bindBacklogSize: int
    interestOpQueued: boolean
    keepAlive: boolean
    linger: int
    maxConnPerSegmentstore: int
    reuseAddr: boolean
    rcvBuf: int
    sndBuf: int
    tcpNoDelay: boolean
    timeoutMillis: int
    ssl: boolean
    uri:
      schema: string
    node:
      addrs: list
      port: int
      conn:
        attemptsLimit: int
        pooling: boolean
